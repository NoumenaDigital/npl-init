package counter;

@api
protocol[user] Counter(var target: Number) {

    initial state counting;
    state done;

    var i: Number = 0;

    @api
    permission[user] increment()  {
        i = i + 1;
        if (i >= target) {
            become done;
        };
    };
};
